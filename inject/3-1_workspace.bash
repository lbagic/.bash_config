#!/bin/bash
ws="~/workspace"
alias ws="cd ${ws}"
cfg="~/.bash_config"
alias cfg="cd ${cfg}"
skel="${ws}/skel"
alias skel="cd ${skel}"

pkg="${ws}/packages"
alias pkg="cd ${pkg}"
p="${ws}/projects"
alias p="cd ${p}"
s="${ws}/sintezis"
alias s="cd ${s}"

echo "workspace: ws, cfg, pkg, p, s, skel"
