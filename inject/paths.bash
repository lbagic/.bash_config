#!/bin/bash
ws="/mnt/d/workspace"
alias ws="cd ${ws}"
cfg="${ws}/cfg"
alias cfg="cd ${cfg}"

# MAIN
pkg="${ws}/packages"
alias pkg="cd ${pkg}"
proj="${ws}/projects"
alias proj="cd ${proj}"
s="${ws}/sintezis"
alias s="cd ${s}"

echo "paths: ws, cfg, pkg, proj, s"
