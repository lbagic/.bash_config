#!/bin/bash
alias ga="git add ."
alias gc="git commit -m"
alias gs="git status"
alias gm="git merge"
alias gck="git checkout"
alias gpush="git push"
alias gpusho="git push --set-upstream origin"
alias gpull="git pull"

echo 'git: ga, gc, gs, gck, gpull, gpush, gpusho'
